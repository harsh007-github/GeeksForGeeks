//Program to multiply elements of an array
class Solution{
    public:
    int product(int arr[], int n)
    {
     int answer=1;
     for(int i=0;i<n;i++)
     {
         answer=answer*arr[i];
     }
        return answer;
        
    }
};
